%h3= t :my_account_settings

= render :partial => "identities/index", :locals => {:identities => @identities }

%p= link_to t(:add_new_identity), new_user_identity_path(current_user)

%p= render '/shared/social_links'

%p= link_to t(:merge_with_my_other_account), new_merge_path