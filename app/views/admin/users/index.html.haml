%h3= t(:listing_users)

= render :partial => "admin/users/search_form"

= paginate @users

= render :partial => "users", :locals => {:users => @users}
          
= paginate @users
          
          